#tcp/ip具体含义
 In the literal sense, one might think that TCP/IP protocol refers to both TCP and IP protocols.There are
 two kinds of agreements actually.In many cases,however,it's simply a general term of for a group of protocols that must be used
 when communicating with Ip.To be specific, IP or ICMP, TCP or UDP, TELNET or FTP, and HTTP,belong to TCP/IP protocol.
 They with long and close ties to TCP or IP.